#Auto generated by Edalize

TOPLEVEL = top_module
ANALYZE_OPTIONS = --std=08 some analyze_options
RUN_OPTIONS = a few run_options

all: analyze

run:
	ghdl --elab-run $(TOPLEVEL) $(RUN_OPTIONS) $(EXTRA_OPTIONS)
analyze:
	ghdl -a $(ANALYZE_OPTIONS) vhdl_file.vhd
	ghdl -a $(ANALYZE_OPTIONS) --work=libx vhdl_lfile
	ghdl -a $(ANALYZE_OPTIONS) vhdl2008_file
