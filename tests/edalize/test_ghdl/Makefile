#Auto generated by Edalize
STD = --std=08
TOPLEVEL = top_module
ANALYZE_OPTIONS = some analyze_options
RUN_OPTIONS = a few run_options

all: analyze

run:
	ghdl --elab-run $(STD) $(TOPLEVEL) $(RUN_OPTIONS) $(EXTRA_OPTIONS)
analyze:
	ghdl -a $(STD) $(ANALYZE_OPTIONS) vhdl_file.vhd
	ghdl -a $(STD) $(ANALYZE_OPTIONS) --work=libx vhdl_lfile
	ghdl -a $(STD) $(ANALYZE_OPTIONS) vhdl2008_file
